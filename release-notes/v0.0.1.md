## v0.0.1 - Initial Release

### Features

- **Statistics command** — Get call statistics via `/v1/statistics/` endpoint
- **Core commands** — balance, sip list, did list, sms send, pbx info
- **JSON output** — Use `--json` flag for machine-readable output
- **HMAC-SHA1 auth** — Proper Zadarma API authentication
