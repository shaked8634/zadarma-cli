## v0.0.4 - README Cleanup

### Improvements

- Simplified README — removed `./` prefix, debug section, testing section
- Cleaner `did list` output — `+14155551234 (mobile)` instead of `DID: +... (Type: ...)`

### Full Changelog

```
8ce20aa docs: simplify README and clean up did list output
```
